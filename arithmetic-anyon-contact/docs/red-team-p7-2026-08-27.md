# P7 Post-Publication Adversarial Audit — QNFO.RES.028 (2026-08-27)

Audit of the published record **10.5281/zenodo.22124744** (concept 10.5281/zenodo.22124743,
v1.0.0) and its distribution stores, run after publication per the publish-then-audit loop.

## Method

Direct parent-agent audit with live tool verification of every store, plus one reviewer
slot dispatched (queued at audit time; the direct audit is authoritative where the slot
did not complete). All claims below carry same-turn API evidence
(`/tmp/p7_record.log`, `/tmp/p7_head_integrity.log`, `/tmp/p7_diff.log`, `/tmp/p7_nl_check.log`
at audit time; log paths are local scratch, not deposited).

## Findings

### 1. Publication integrity — CLEAN

- Zenodo records API: status `published`, doi `10.5281/zenodo.22124744`, 29 files.
- related_identifiers: 5 (isSupplementTo GitHub branch + 4 cites), all present.
- license cc-by-4.0 (API view), creator `Quni-Gudzinas, Rowan Brad` with ORCID
  0009-0002-4317-5604, 9 keywords.
- DataCite: state `findable`, title matches.
- doi.org GET with browser UA: 200 → zenodo.org/records/22124744. (A HEAD request
  with a curl-class UA returns 404 — bot-block artifact, differential-proven against
  the parent record; DOIDOT-403-BOT-1.)
- Deposited md frontmatter: `doi: 10.5281/zenodo.22124744`, `version: 1.0.0` —
  self-DOI assert PASS (POST-PUBLISH-FRONTMATTER-ASSERT-1).

### 2. Repository state at the P5 head (401f0a6) — CLEAN

- 33 files committed: paper md/html/pdf, references.bib, citation-audit.md, README.md
  (How-to-Cite with concept DOI), LICENSE (CC BY 4.0 legalcode), PROJECT-PLAN.md,
  4 build scripts, full verification suite (3 scripts + 3 logs + 3 JSONs + README),
  UIA, due-diligence, consilience gate, reconciliation, 3 external-search evidence
  files, .gitignore, .gitkeep x3.
- Content-level checks at HEAD: README How-to-Cite present; citation-audit 22/22 with
  both corrected DOIs; paper carries all 22 citations + bibliography field; bib
  carries the three corrected DOIs (spector1990 10.1007/BF02096755, bakas1991
  10.1063/1.529511, gentile1940 10.1007/BF02960187). No stale versions committed.

### 3. Cross-store content integrity — CLEAN (byte-verified)

- Deposited md vs R2 mirror vs committed md: identical after newline normalization
  (24,497 B each). The raw 187-byte delta between the fresh checkout and the
  deposited copy is exactly 187 CRLF line endings introduced by git autocrlf on
  checkout (the commit itself carried the LF-normalized blob; the warning
  "LF will be replaced by CRLF" was printed at commit time). WRITE-TEXT-NEWLINE-1
  artifact, not a content defect. No remediation required.

### 4. Distribution stores — CLEAN

- R2 `qnfo-releases/2026/08/arithmetic-anyon-contact/`: 29 objects, md download-back
  md5-verified.
- D1 living-paper: row present, body_md 24,434 chars (matches the committed paper),
  status published.
- KG: `paper:arithmetic-anyon-contact` distributed, verdicts C1/C2/H2 confirmed.
- Vectorize: paper is the top semantic hit for its own query (score 0.715, multiple
  chunks). The global queue cursor is stalled at 300/509 — a pre-existing
  corpus-queue condition (the same stall noted in the RES.023 cycle), not caused by
  this paper.

## Soft notes (documentation-level, no action)

1. doi.org HEAD requests from bot-class UAs return 404 while GET with browser UA
   resolves — verification scripts should use GET with browser UA.
2. The global Vectorize queue stall predates this paper; a corpus-level queue repair
   remains open for the infrastructure owner.
3. PDF MediaBox is 595.92 x 841.92 pt (Chromium's sub-millimeter A4 emission),
   10 pages, 0 U+FFFD in decompressed text streams, no browser chrome
   (displayHeaderFooter:false).

## Verdict

**0 HARD / 3 SOFT.** The published record, the repository head, and all distribution
stores are consistent and complete. No remediation required.
