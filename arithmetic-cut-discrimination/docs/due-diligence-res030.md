# Due-Diligence Report — QNFO.RES.030 Phase 1

Date: 2026-08-29. Corpus: 1,022 papers (records fleet), KG 8,336 nodes / 1,670 Paper.
Scope: DUE-DILIGENCE-DEPTH-1 — 11 formulations / 4 topics, limit 20, per-hit ID
validation, ≥2 adjacent domains, external independent verification.

## Corpus map

The core spine for the arithmetic cut is fully registered and distributed:
RES.027 (22133122), RES.028 (22124744), RES.029 (22142794), plus RES.020
(22035210), RES.021 (22046458), and UMP.014 (22150472) which supplies the
estimation machinery and the premise boundary. Adjacent domains surface
records that constrain the project: UMP (adelic QEC 21979060-area, QEC
threshold 2.0e-4, acrp08 paradigm forecast), CMP/JPC (qwave-qudit-advantage
21880104, ringbauer due diligence, joules-per-solution), SR (noise-augmented
SRE — adjacency only), and methodology records (radix-agnostic-dsi-detection
21902891 certified-null protocol; scientific-validity-assessment-toolkit;
spectral-benchmarking-of-holographic-quantum-simulations).

## Records that contradict or complicate (surfaced, not suppressed)

1. **prime-numbers-as-spectral-artifacts** (10.5281/zenodo.17566147, 2025-11):
   claims primes "emerge as spectral artifacts" from a continuous substrate
   "with logical necessity." Stronger than anything the 2026 register supports;
   the 2026 null-ledger (UMP.014 §3) undercuts the geometric form of that
   claim. For RES.030 it matters methodologically: if primes are spectral
   artifacts of some continuous system, the matched-density nulls must include
   *continuous-derived* spectra, not only log-spaced surrogates. Folded into
   the null-ensemble design (P2).
2. **Dueñas–Svaiter randomized Riemann gas** (arXiv:1401.8190): randomization
   over an ensemble of Hamiltonians removes the zeta-pole singularity in the
   non-enumerable ensemble. Complication: ensemble averages can wash out the
   very signature RES.030 seeks to discriminate — the discrimination protocol
   must compare single realizations and ensemble means separately (P2 design
   constraint, already anticipated by the form-factor non-self-averaging note
   in the UIA Q3).
3. **UMP.014 §10 limitations**: null MC at n=2000 vs real n≈200,000 — the
   N-dependence of the test statistic is weak but not zero; RES.030 thresholds
   must be reported as functions of both P_max and sample/ensemble size.

## Gap analysis (the project's warrant)

- **G1 — finite-P_max Bost–Connes rounding.** No corpus record treats the
  finite-cutoff rounding of the β=1 transition (width ~1/ln P_max) or states
  the honest falsification criterion for a "transition" claim at finite P_max.
  Only the provenance chats carry it. RES.030 closes this.
- **G2 — matched-level-density discrimination.** No record constructs
  matched-density non-arithmetic nulls for the specific heat. Methodology
  exists (certified-null protocol, SVAT, spectral benchmarking); the
  application to the primon gas does not. This is the project's core.
- **G3 — Maxwell–Boltzmann third statistics.** Present in the provenance chats
  (Z_MB = exp(Σ p^{-β}), Li(e^{-β}) limit), never published. RES.030 carries
  it into the registered corpus.
- **G4 — external primon-gas lineage.** Verified external literature (Julia
  1989/1990; Bakas–Bowick 1991; Spector 1990; Dueñas–Svaiter 2014; p-adic
  kernels 2024; Hartnoll–Yang 2025) is not carried in the visible bodies of
  RES.027–029. RES.030 will carry it explicitly (P2/P5).
- **G5 — information location.** Whether the distinguishing information lives
  in C_V(β) or only in the two-point statistics is open in every record.
  RES.030's D2 adjudicates it.
- **G6 — practitioner thresholds.** No record ties the primon-gas observable
  to platform-feasibility constraints (mode count, noise). D3 delivers the
  threshold tables the platform records need.

## SO-WHAT convergence

The corpus itself demonstrates the reader-care requirement: UMP.014 ships "a
machine, not a conclusion"; RES.028's outreach queue shows live engagement with
the external randomized-Riemann-gas community (Svaiter). RES.030 extends the
machine to discrimination: a spectroscopist gets a go/no-go with thresholds
before any platform is built; a number-theorist gets a computational statement
of which spectral function carries the prime-gap information; either the
specific-heat observable survives the program's own red-team objection or it
is corrected to the two-point function — actionable in both branches.

## Premise-depth convergence

No premises beyond the inherited boundary are added: Boltzmann weights with
β∈R; no p-adic amplitude; no physical length at a p-adic place (L0).
Occupation rules and null definitions are constructive (L1); Euler products
and thermodynamic derivatives are derived and exact (L2); the spectral
estimators are imported named anchors re-verified in code (L3); D1/D2 carry
the risk (L4) with the pre-registered disconfirmation matrix.
