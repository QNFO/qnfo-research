targets.json`:
  ```json
  {
    "mass_ratios": ["mp_me", "mmu_me", "mtau_me", "mt_me", "mb_me", "mc_me"],
    "values": [1836.15267343, 206.7682830, 3477.23, 345000, 8270, 2060],
    "yukawas": ["ye", "ymu", "ytau"],
    "yukawa_values": [2.94e-6, 6.07e-4, 0.01021],
    "weak_ratio": 0.8814
  }
  ```
- [ ] Confirm CODATA 2018/2022 constants file `constants.py` is correct.

---

## Agent Role Definitions

| Agent | Capabilities | Output Format |
|-------|-------------|----------------|
| `CodeAgent` | Writes Python scripts; runs them; returns outputs and logs. | `.py` file + stdout/stderr as text. |
| `SearchAgent` | Queries arXiv (via API) and Google Scholar (via `scholarly` or manual prompts). Returns list of papers with titles, authors, summaries. | Markdown table. |
| `WriteAgent` | Composes text sections, abstracts, figure captions, READMEs. | Markdown or plain text. |
| `VerifyAgent` | Cross‑checks numerical calculations, logical consistency, citation accuracy. Returns pass/fail + notes. | Bullet list. |

Human interacts via a shared chat log (e.g., a Markdown file `sprint_log.md` where tasks are posted and results appended).

---

## Day 1 – Morning (4 hours): Monte Carlo & Near‑Match Assessment

### 08:00 – 08:30 | Human setup
- Create `sprint_log.md` with headers for each session.
- Post task to `CodeAgent`: “Write script `monte_carlo_near_matches.py` that implements the following specification.”

**Specification for CodeAgent (to be copied into prompt):**
```python
"""
Input: targets.json (contains yukawa_values and weak_ratio), tolerances list [0.0093, 0.0082, 0.0089, 0.0089].
Define n_ratios = 600, n_targets = 4 (ye, ymu, ytau, mW/mZ).
Log range: from ln(1e-6) to ln(1e22) (approx -13.8 to 50.7).
Null hypothesis: ratios are uniformly distributed in log space.
For each trial (1 million):
  - Generate n_ratios random log-uniform numbers.
  - Convert to linear ratios.
  - For each target, count how many ratios fall within relative tolerance.
  - Sum counts a