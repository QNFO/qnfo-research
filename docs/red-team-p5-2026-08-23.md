# Red-Team Review — QNFO.RES.022 P5 (2026-08-23)

- **Audited artifact:** paper v0.2 (`keyword-taxonomy-consilience.md`) + verification suite, branch `res/paper/keyword-taxonomy-consilience` @ **aa9f034** (tag v0.4-phase4-res022)
- **Protocol:** CMD RED TEAM — 3 parallel reviewer slots (Accuracy / Completeness / Dependency), all completed with full handoffs; READ-ONLY (no artifact modified)
- **Scope:** paper v0.2, PROJECT-PLAN.md, artifacts/verification/* (rq1/rq3/rq4/rq5 scripts+results+logs, verification-summary.md, corpus_qnfo_titles.json, keyword-taxonomy-source.md), artifacts/due-diligence-phase1.md, artifacts/p2-consilience-map.*, artifacts/external-search/arxiv-evidence-2026-08-23.json

## Aggregate verdict

**Number chain: CLEAN (all three reviewers independently verified every quantitative claim in the paper against the deposited JSONs/logs — 12/12 Table 3 rows, Tables 1–2, L3/L4 counts, corpus stats, DOIs, arXiv IDs).** The HARD findings are paper-text defects, not verification-suite defects; no suite regeneration is needed. Publish is BLOCKED until the §4.4 enumerations and the RQ2/HNSW claim scoping are corrected.

## HARD findings (aggregated, deduplicated)

| # | Finding | Reviewers | Fix |
|:-:|:--------|:----------|:----|
| H1 | **§4.4 keyword enumerations contradict the deposited rq5 output.** All three overlap lists are wrong (counts 5/5/4 right, members wrong): Agentic→PLT claimed `mcp-server, mcp-tool, mcp-client, mcp-transport, mcp-resource`; actual `fastmcp, mcp-client, mcp-server, mcp-tool, model-context-protocol`. 4-D→PLT claimed `zenodo, zenodo-api, zenodo-upload, zenodo-doi, invenio`; actual `arweave, ipfs, nft-storage, web3-storage, zenodo`. Measurement→RES claimed `measurement-stratigraphy, measurement-foundations, quantum-measurement, einselection`; actual `einselection, epistemology-of-measurement, measurement-stratigraphy, operationalism`. The "MCP family" characterization is wrong — `mcp-transport`/`mcp-resource` occur only in PLT, never in the cross-cutting section. | Accuracy, Dependency, Completeness (3/3) | Copy the JSON overlap lists verbatim from `rq5_results.json → L4_cross_cutting.*.overlap_keywords`; correct the "MCP family" sentence |
| H2 | **RQ2 never implemented.** PROJECT-PLAN's RQ2 (do cross-domain consilience links share p-adic prefixes more often than cosine similarity above threshold?) has no test anywhere; `rq1_retrieval_benchmark.py` is labeled "RQ1/RQ2" but computes only p@5/p@10/MRR. The paper silently drops RQ2 with no disclosure. | Completeness | Implement the RQ2 test or explicitly re-scope/delete RQ2 in plan + paper (disclose) |
| H3 | **"cosine/HNSW" + "two public corpora" unverified.** Abstract/§1/§5/§6 claim benchmarks "against cosine/HNSW baselines" "on two public corpora"; the suite has NO HNSW comparator and NO public corpora (A = seeded synthetic, B = internal QNFO titles). "At equal build cost" and the latency observable are also unmeasured. | Dependency, Completeness, Accuracy (3/3) | Amend claims to "cosine baseline on two pinned corpora (one synthetic, one internal title-only)" or add the HNSW benchmark before P6 |
| H4 | **Deliverable 1 identity collision.** §6 presents Deliverable 1 as the p-adic fingerprint index "benchmarked head-to-head against cosine/HNSW", while §4.6/§9 treat the naive sha256→p-adic hash as the encoding CONTROL that provably destroys metric structure (0.21 vs 1.00). The paper must resolve which object Deliverable 1 is (the data-derived ultrametric index is the H1 index; the hash variant is the control). | Dependency | Rewrite §6 Deliverable 1 to name the data-derived ultrametric index as the deliverable and the hash variant as its control; align with §4.6/§9 |

## SOFT findings (aggregated)

1. **Table 1 sums to 338 vs abstract's 335** — raw vs normalized counting never explained (2 intra-program collisions p-adic/padic, adele/adeles + cross-program complexity-measure account for +3). Add a footnote. (Accuracy, Completeness)
2. **`artifacts/p2-consilience-map.md` says "335 OWNS edges"** but the JSON provably contains 336 (complexity-measure has both INM and RES edges); the paper's "342 nodes, 336 ownership edges" is correct — fix the map README. (Accuracy, Dependency)
3. **§4.3 lists 5 of the 6 Cross-Domain Bridges keywords** — compton-bt omitted. (Accuracy)
4. **corpus_qnfo_titles.json contains junk/truncated/duplicate rows** ("Modern Physics Metrology]]" x≥6, ~20 "Strange Loop of Being" chapter fragments, ~10 "Philosophy of Science", duplicate slug adelic-core-synthesis, truncated slug autaxys-and-its-generative-engin) with no cleaning disclosed in §10. Disclose the cleaning rule (title ≥ 15 chars, junk filter) in §10. (Completeness)
5. **Corpus-B label circularity not discussed in §9** — the program labels are derived from the taxonomy under audit (majority-vote keyword match on titles); the paper should disclose that the corpus-B ground truth is taxonomy-derived. (Completeness)
6. **p2-consilience-map.md "12,684 B" fetch-size claim** inconsistent with the 12,988-char archived source — reconcile at P6 against the canonical file. (Dependency, Completeness)
7. **G5 (p-adic-ML/hyperbolic/Anashin literature sweep) never executed** and §8 omits it; the due-diligence gap remains open — either execute in P6 or disclose as an open limitation in §9. (Completeness)
8. **F2: cited atlas 10.5281/zenodo.21722395 sits under a known KG/D1 conflict** (KG node KILLED vs D1 published) — verify at the P6 citation audit before citing. (Completeness)
9. **§8 "adelic core synthesis" unnamed record** needs a DOI or removal from the related-work paragraph. (Dependency)
10. **Refs 11–13 (Parisi / Rammal–Toulouse–Virasoro / Vladimirov et al.)** are not in local evidence files but are canonical literature — acceptable, note at P6 citation audit. (Accuracy, informational)
11. **PROJECT-PLAN RQ1 is stale** — describes a "p-adic hash prefix" index while the delivered v2 uses data-derived single-linkage with the hash as control (paper is correct, plan stale). Update the plan. (Accuracy, informational)
12. **PUBLICATION-SOURCE-COMPLETENESS-1 items pending** (references.bib, citation-audit.md, README.md) — P6 deliverable, not a current failure. (Completeness)

## Verified clean (reviewer-confirmed)

- All quantitative claims: 335/334/99.7%/, complexity-measure INM∩RES, contingency (0,53,0,282), precision 0, Fisher p=1.0 — match rq5 JSON/log exactly
- Table 1 counts and bridge shares; Table 2 family coverage; L3 counts (7/7/6/9); L4 counts (50→5, 26→5, 23→4)
- Table 3: all RQ1/RQ3/RQ4 numbers == run logs == results JSONs (12/12 rows)
- Corpus stats (8,324 nodes / 1,660 papers) consistent across §2/§10 and due-diligence
- All 9 arXiv IDs present in evidence file with matching titles/authors/years; all 21 references cited, no orphans; all internal anchors resolve
- Scripts use relative paths, deterministic seeds — byte-identical regeneration claim sound
- SO-WHAT gate (§1/§7), PRACTITIONER-RELEVANCE (§6), disconfirmation criteria + 2028 decision point (§5), UIA 15Q, VERIFY-FIX-RERUN-1 documented, F1/F3 citation hazards avoided, G1–G4/G6 disclosed

## Remediation plan (next cycle, P5.1)

1. H1: replace §4.4 enumerations verbatim from rq5_results.json; fix "MCP family" sentence.
2. H2: implement the RQ2 valuation-depth-vs-cosine test in rq1 (extend script, regenerate results) OR re-scope RQ2 in plan+paper with disclosure.
3. H3: amend H1/Deliverable-1 claims to the actual baseline set (cosine + single-link ultrametric + hash control on two pinned corpora: synthetic + internal titles) OR add HNSW before P6.
4. H4: rewrite §6 Deliverable 1 to name the data-derived ultrametric index as deliverable, hash as control.
5. SOFT 1–7: footnote Table 1 counting; fix map-README 335→336; add compton-bt; disclose corpus cleaning in §10; disclose label circularity in §9; reconcile 12,684/12,988 at P6; execute or disclose G5.
6. Re-run publication gates (banned tokens, superscripts, frontmatter) after edits; commit as P5.1 remediation (tag v0.5-phase5-res022), then P6 deposit.
